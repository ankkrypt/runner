name: 24-7 Mumbai VM Grabber
on:
  schedule:
    - cron: '*/30 * * * *' # Tries to start a new job every 30 minutes
  workflow_dispatch:      # Allows you to start it manually

jobs:
  loop-request:
    runs-on: ubuntu-latest
    # This ensures only one script runs at a time
    concurrency: 
      group: oci-grabber
      cancel-in-progress: true
      
    steps:
      - name: Install OCI CLI
        run: |
          pip install oci-cli
          echo "/home/runner/.local/bin" >> $GITHUB_PATH

      - name: Configure OCI CLI
        run: |
          mkdir -p ~/.oci
          echo "[DEFAULT]" > ~/.oci/config
          echo "user=${{ secrets.OCI_USER_OCID }}" >> ~/.oci/config
          echo "fingerprint=${{ secrets.OCI_FINGERPRINT }}" >> ~/.oci/config
          echo "tenancy=${{ secrets.OCI_TENANCY_OCID }}" >> ~/.oci/config
          echo "region=ap-mumbai-1" >> ~/.oci/config
          echo "key_file=~/.oci/key.pem" >> ~/.oci/config
          
          # Write the private key from secrets to the key file
          echo "${{ secrets.OCI_PRIVATE_KEY }}" > ~/.oci/key.pem
          chmod 600 ~/.oci/key.pem
          
          # Suppress the permission warnings in logs
          echo "OCI_CLI_SUPPRESS_FILE_PERMISSIONS_WARNING=True" >> $GITHUB_ENV

      - name: Persistent Loop (25 minutes)
        run: |
          # This runs for 25 minutes (1500 seconds)
          end=$((SECONDS+1500))
          while [ $SECONDS -lt $end ]; do
            echo "Attempting to launch at $(date)"
            
            # Trigger the Stack Apply Job
            # The '|| true' keeps the script running even if Oracle returns an error
            oci resource-manager job create-apply-job \
              --stack-id ${{ secrets.OCI_STACK_OCID }} \
              --execution-plan-strategy AUTO_APPROVED || true
            
            echo "Waiting 2 minutes (120s) to avoid API rate limits..."
            sleep 120
          done
